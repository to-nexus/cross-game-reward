# CreateProject.s.sol 환경변수 예제
# 새로운 스테이킹 프로젝트를 생성합니다

# StakingProtocol 주소 (Deploy.s.sol 실행 결과)
STAKING_PROTOCOL_ADDRESS=0xYourStakingProtocolAddress

# 프로젝트 이름 (고유해야 함)
PROJECT_NAME="My Staking Project"

# 시즌 길이 (블록 수)
# 0 = 기본값 사용 (2,592,000 블록 ≈ 30일, 1초/블록 기준)
# 예: 86400 = 1일 (1초/블록)
SEASON_BLOCKS=86400

# 첫 시즌 시작 블록 번호
# 현재 블록보다 미래여야 함
# 예: 현재 블록 + 100 (약 100초 후 시작)
FIRST_SEASON_START_BLOCK=1000000

# 풀 종료 블록 (0이면 무한)
# 예: 0 = 영구적으로 운영
POOL_END_BLOCK=0

# 프로젝트 관리자 주소
PROJECT_ADMIN=0xYourProjectAdminAddress

# 사용 예시:
# 방법 1: Private Key 직접 전달
# source script/CreateProject.env
# forge script script/CreateProject.s.sol:CreateProjectScript \
#     --rpc-url $RPC_URL \
#     --private-key $PRIVATE_KEY \
#     --broadcast -vvvv
#
# 방법 2: Keystore 사용 (권장)
# forge script script/CreateProject.s.sol:CreateProjectScript \
#     --rpc-url $RPC_URL \
#     --account my_account \
#     --sender 0xYourAddress \
#     --broadcast -vvvv
