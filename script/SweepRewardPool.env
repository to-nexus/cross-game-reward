# RewardPool Sweep 스크립트 환경변수

# === Mode 1: RewardPool 주소로 직접 Sweep ===
# STAKING_PROTOCOL_ADDRESS=0x...    # StakingProtocol 주소
# REWARD_POOL_ADDRESS=0x...         # RewardPool 주소
# TOKEN_ADDRESS=0x...               # 회수할 토큰 주소
# SWEEP_AMOUNT=1000000000000000000  # 회수할 수량 (wei 단위, 예: 1 token = 1e18)
# 수신자 주소는 명령줄 플래그로 전달: --sig "run(address)" 0x수신자주소

# === Mode 2: Project ID로 Sweep (권장) ===
STAKING_PROTOCOL_ADDRESS=0x...    # StakingProtocol 주소
PROJECT_ID=1                       # 프로젝트 ID
TOKEN_ADDRESS=0x...               # 회수할 토큰 주소
SWEEP_AMOUNT=1000000000000000000  # 회수할 수량 (wei 단위)
# 수신자 주소는 명령줄 플래그로 전달: --sig "run(address)" 0x수신자주소

# === 사용 예시 ===
# 
# 1. Mode 1 (RewardPool 직접 지정):
# source script/SweepRewardPool.env
# forge script script/SweepRewardPool.s.sol:SweepRewardPoolScript \
#     --sig "run(address)" 0x수신자주소 \
#     --broadcast \
#     --rpc-url $RPC_URL
#
# 2. Mode 2 (Project ID 사용 - 권장):
# source script/SweepRewardPool.env
# forge script script/SweepRewardPool.s.sol:SweepRewardPoolByProjectIDScript \
#     --sig "run(address)" 0x수신자주소 \
#     --broadcast \
#     --rpc-url $RPC_URL

# === 네트워크 설정 ===
RPC_URL=https://testnet.crosstoken.io:22001
PRIVATE_KEY=0x...                 # Protocol Admin Private Key
