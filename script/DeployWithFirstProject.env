# DeployWithFirstProject.s.sol 환경변수 예제
# 전체 시스템 배포 + 첫 프로젝트 생성 + 선택적 리워드 예치

# ============================================
# 필수: 프로젝트 설정
# ============================================

# 프로젝트 이름
PROJECT_NAME="My First Staking Project"

# 시즌 길이 (초 단위, 0이면 프로토콜 기본값 사용)
# 예: 86400 = 1일, 604800 = 1주일, 2592000 = 30일
SEASON_DURATION=86400

# 첫 시즌 시작 타임스탬프 (Unix timestamp)
# 예: 1704067200 = 2024-01-01 00:00:00 UTC
# date -u +%s 명령어로 현재 타임스탬프 확인 가능
FIRST_SEASON_START_TIME=1704067200

# 사전 예치 시작 타임스탬프 (0이면 비활성화)
# 예: 첫 시즌 시작 7일 전
PRE_DEPOSIT_START_TIME=0

# 풀 종료 타임스탬프 (0이면 무한)
POOL_END_TIME=0

# ============================================
# 선택: WCROSS
# ============================================

# 기존 WCROSS 주소가 있으면 재사용 (없으면 새로 배포)
# WCROSS_ADDRESS=0xExistingWCROSSAddress

# ============================================
# 선택: 프로젝트 관리자
# ============================================

# 프로젝트 관리자 주소 (없으면 배포자가 관리자)
# PROJECT_ADMIN=0xProjectAdminAddress

# ============================================
# 선택: 리워드 토큰 예치
# ============================================

# 리워드 토큰 주소 (설정하면 자동으로 예치)
# REWARD_TOKEN=0xYourRewardTokenAddress

# 리워드 수량 (wei 단위)
# 예: 1000000000000000000 = 1 token (18 decimals)
# REWARD_AMOUNT=1000000000000000000

# 리워드를 예치할 시즌 (기본값: 1)
# REWARD_SEASON=1

# ============================================
# 사용 예시
# ============================================

# 방법 1: 리워드 없이 배포
# forge script script/DeployWithFirstProject.s.sol:DeployWithFirstProjectScript \
#     --rpc-url $RPC_URL \
#     --private-key $PRIVATE_KEY \
#     --broadcast --verify -vvvv

# 방법 2: 리워드 포함 배포
# REWARD_TOKEN=0x... REWARD_AMOUNT=1000000000000000000 \
# forge script script/DeployWithFirstProject.s.sol:DeployWithFirstProjectScript \
#     --rpc-url $RPC_URL \
#     --private-key $PRIVATE_KEY \
#     --broadcast --verify -vvvv

# 방법 3: 환경변수 파일 사용
# source script/DeployWithFirstProject.env
# forge script script/DeployWithFirstProject.s.sol:DeployWithFirstProjectScript \
#     --rpc-url $RPC_URL \
#     --private-key $PRIVATE_KEY \
#     --broadcast --verify -vvvv

# ============================================
# 타임스탬프 변환 도구
# ============================================

# 현재 타임스탬프 확인:
# date -u +%s

# 날짜를 타임스탬프로 변환:
# date -u -d "2024-01-01 00:00:00" +%s  # Linux
# date -u -j -f "%Y-%m-%d %H:%M:%S" "2024-01-01 00:00:00" "+%s"  # macOS

# 타임스탬프를 날짜로 변환:
# date -u -d @1704067200  # Linux
# date -u -r 1704067200  # macOS
