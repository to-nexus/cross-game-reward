# Deploy.s.sol 환경변수 예제
# 전체 시스템을 배포합니다 (WCROSS, Code 컨트랙트, StakingProtocol)

# 프로토콜 관리자 주소 (DEFAULT_ADMIN_ROLE을 가질 주소)
PROTOCOL_ADMIN=0xYourProtocolAdminAddress

# (선택) 기존 WCROSS 주소가 있으면 재사용, 없으면 새로 배포
# WCROSS_ADDRESS=0xExistingWCROSSAddress

# 사용 예시:
# 방법 1: Private Key 직접 전달
# forge script script/Deploy.s.sol:DeployScript \
#     --rpc-url $RPC_URL \
#     --private-key $PRIVATE_KEY \
#     --broadcast --verify -vvvv

# 방법 2: Keystore 사용 (권장)
# forge script script/Deploy.s.sol:DeployScript \
#     --rpc-url $RPC_URL \
#     --account my_account \
#     --sender 0xYourAddress \
#     --broadcast --verify -vvvv

# 방법 3: Ledger 하드웨어 월렛
# forge script script/Deploy.s.sol:DeployScript \
#     --rpc-url $RPC_URL \
#     --ledger \
#     --sender 0xYourAddress \
#     --broadcast --verify -vvvv
