# DeployRouter.s.sol 환경변수 예제
# StakingRouter를 배포하고 프로젝트에 승인합니다

# WCROSS 주소
WCROSS_ADDRESS=0xYourWCROSSAddress

# StakingProtocol 주소
STAKING_PROTOCOL_ADDRESS=0xYourStakingProtocolAddress

# Router를 승인할 프로젝트 ID
PROJECT_ID=1

# 사용 예시:
# 방법 1: Private Key 직접 전달
# source script/DeployRouter.env
# forge script script/DeployRouter.s.sol:DeployRouterScript \
#     --rpc-url $RPC_URL \
#     --private-key $PRIVATE_KEY \
#     --broadcast -vvvv
#
# 방법 2: Keystore 사용 (권장)
# forge script script/DeployRouter.s.sol:DeployRouterScript \
#     --rpc-url $RPC_URL \
#     --account my_account \
#     --sender 0xYourAddress \
#     --broadcast -vvvv

# 주의: 프로젝트 admin 또는 프로토콜 admin 권한이 필요합니다
