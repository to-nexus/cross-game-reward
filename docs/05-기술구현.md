# 기술 구현 디테일

## 1. Code 컨트랙트 패턴 + CREATE2

### 1.1 문제: 컨트랙트 크기 제한

**Ethereum 제약:**
- 컨트랙트 최대 크기: 24KB (EIP-170)
- StakingProtocol + StakingPool + RewardPool = 크기 초과

### 1.2 해결: Code 패턴 + CREATE2

**개념:**
```
StakingPoolCode 컨트랙트:
- StakingPool의 creation code만 저장
- code() 함수로 반환

StakingProtocol:
- Code 컨트랙트에서 creation code 가져오기
- Constructor 인자와 결합
- CREATE2 opcode로 배포 (주소 예측 가능)
```

**구현:**

```solidity
// Code 컨트랙트
contract StakingPoolCode {
    function code() external pure returns (bytes memory) {
        return type(StakingPool).creationCode;
    }
}

// Factory에서 사용 (CREATE2)
function _deployStakingPool(
    uint projectID,
    string calldata projectName,
    uint seasonBlocks,
    uint firstSeasonStartBlock,
    uint poolEndBlock
) internal returns (address pool) {
    // 1. Code 가져오기
    bytes memory code = _getStakingPoolCode();
    
    // 2. Constructor 인자 인코딩
    bytes memory constructorArgs = abi.encode(
        projectID,
        address(_crossToken),
        address(this),
        seasonBlocks,
        firstSeasonStartBlock,
        poolEndBlock
    );
    
    // 3. 결합
    bytes memory bytecode = bytes.concat(code, constructorArgs);
    
    // 4. Salt 생성 (deterministic)
    bytes32 salt = keccak256(abi.encode(projectName, projectID, "StakingPool"));
    
    // 5. CREATE2로 배포
    assembly {
        pool := create2(0, add(bytecode, 0x20), mload(bytecode), salt)
    }
    
    require(pool != address(0), "Deployment failed");
}
```

**장점:**
1. Factory 크기 감소
2. 업그레이드 용이성
3. 배포 비용 최적화
4. **주소 예측 가능성** (CREATE2)
   - 배포 전 주소 계산 가능
   - 크로스체인 배포 시 동일 주소 보장
   - 프론트엔드 UX 향상

### 1.3 CREATE2 주소 계산

**CREATE2 주소 공식:**
```
address = keccak256(0xff ++ deployer ++ salt ++ keccak256(init_code))[12:]
```

**구현:**
```solidity
function computeStakingPoolAddress(
    string calldata projectName,
    uint projectID,
    uint seasonBlocks,
    uint firstSeasonStartBlock,
    uint poolEndBlock
) external view returns (address) {
    // 1. Bytecode 구성
    bytes memory code = _getStakingPoolCode();
    bytes memory bytecode = bytes.concat(
        code,
        abi.encode(
            projectID, address(_crossToken), address(this),
            seasonBlocks, firstSeasonStartBlock, poolEndBlock
        )
    );

    // 2. Salt 생성 (배포 시와 동일)
    bytes32 salt = keccak256(abi.encode(projectName, projectID, "StakingPool"));
    
    // 3. CREATE2 주소 계산
    bytes32 hash = keccak256(
        abi.encodePacked(
            bytes1(0xff),
            address(this),  // deployer
            salt,
            keccak256(bytecode)  // init_code_hash
        )
    );

    return address(uint160(uint(hash)));
}
```

**Salt 구조 설계:**
```solidity
// StakingPool
salt = keccak256(abi.encode(projectName, projectID, "StakingPool"))

// RewardPool
salt = keccak256(abi.encode(projectName, projectID, "RewardPool"))
```

**Salt 선택 이유:**
- `projectName`: 사용자가 제공, 고유성 보장
- `projectID`: 시스템에서 부여, 순차적 증가
- `"StakingPool"` / `"RewardPool"`: 타입 구분
- 조합하여 충돌 방지 및 예측 가능성 확보

### 1.4 동적 staticcall 패턴

```solidity
function _getStakingPoolCode() internal view returns (bytes memory) {
    (bool success, bytes memory code) = 
        stakingPoolCodeContract.staticcall(
            abi.encodeWithSignature("code()")
        );
    
    require(success, "Failed to get code");
    return abi.decode(code, (bytes));
}
```

---

## 2. 다중 시즌 롤오버 최적화

### 2.1 문제: 시즌 건너뛰기

**시나리오:**
```
시즌 1 시작: 블록 1000 (길이: 1000블록)
아무도 참여하지 않음
현재 블록: 5500

예상:
- 시즌 1: 1000-2000
- 시즌 2: 2001-3000
- 시즌 3: 3001-4000
- 시즌 4: 4001-5000
- 시즌 5: 5001-6000 (현재 활성)

문제:
- currentSeason = 0 (아무도 참여 안 함)
- 첫 스테이킹 시 어떻게 처리?
```

### 2.2 단순 구현의 문제

**한 번에 하나씩 처리:**
```solidity
function _ensureSeason() internal {
    if (currentSeason == 0) {
        if (block.number >= nextSeasonStartBlock) {
            _startFirstSeason();  // 시즌 1만 생성
        }
        return;  // ❌ 시즌 2, 3, 4, 5는?
    }
    
    Season storage current = seasons[currentSeason];
    if (block.number > current.endBlock) {
        _rolloverSeason();  // ❌ 한 번에 하나만
    }
}
```

**문제점:**
- 첫 트랜잭션: 시즌 1 생성 → `currentSeason = 1`, 블록 2000까지
- 현재 블록 5500 > 2000 → 여전히 시즌 1
- 실제로는 시즌 5여야 함
- 스테이킹 불가능 (시즌이 이미 종료됨)

### 2.3 해결: While 루프 다중 롤오버

**구현:**
```solidity
function _ensureSeason() internal virtual {
    // 풀 종료 체크
    if (poolEndBlock > 0 && block.number >= poolEndBlock) return;

    // 첫 시즌 시작
    if (currentSeason == 0) {
        if (block.number >= nextSeasonStartBlock) {
            _startFirstSeason();  // 시즌 1 생성
            // ↓ 계속 진행하여 추가 롤오버 체크
        } else {
            return;
        }
    }

    // 여러 시즌 롤오버 (최대 50개)
    uint maxRollovers = 50;
    uint rolloversPerformed = 0;

    while (currentSeason > 0 && rolloversPerformed < maxRollovers) {
        Season storage current = seasons[currentSeason];
        
        // 현재 시즌이 아직 진행 중이면 종료
        if (block.number <= current.endBlock) break;

        // 시즌 롤오버
        _rolloverSeason();
        
        unchecked { ++rolloversPerformed; }
    }

    // 50개 초과 시 에러
    require(rolloversPerformed < maxRollovers, "Too many seasons");
}
```

### 2.4 실행 흐름 예시

**시나리오:**
```
블록 1000: 시즌 1 시작 블록
블록 5500: 첫 스테이킹 시도
```

**처리 과정:**
```
1. currentSeason = 0
   → _startFirstSeason() 호출
   → 시즌 1 생성 (1000-2000)
   → currentSeason = 1

2. While 루프 시작:
   
   반복 1:
   - current = seasons[1] (endBlock = 2000)
   - block.number (5500) > 2000 → 롤오버 필요
   - _rolloverSeason() → 시즌 2 생성 (2001-3000)
   - currentSeason = 2, rolloversPerformed = 1
   
   반복 2:
   - current = seasons[2] (endBlock = 3000)
   - block.number (5500) > 3000 → 롤오버 필요
   - _rolloverSeason() → 시즌 3 생성 (3001-4000)
   - currentSeason = 3, rolloversPerformed = 2
   
   반복 3:
   - current = seasons[3] (endBlock = 4000)
   - block.number (5500) > 4000 → 롤오버 필요
   - _rolloverSeason() → 시즌 4 생성 (4001-5000)
   - currentSeason = 4, rolloversPerformed = 3
   
   반복 4:
   - current = seasons[4] (endBlock = 5000)
   - block.number (5500) > 5000 → 롤오버 필요
   - _rolloverSeason() → 시즌 5 생성 (5001-6000)
   - currentSeason = 5, rolloversPerformed = 4
   
   반복 5:
   - current = seasons[5] (endBlock = 6000)
   - block.number (5500) <= 6000 → 현재 시즌 활성!
   - break

3. 최종 상태:
   - currentSeason = 5
   - 시즌 1-4는 finalized
   - 시즌 5 활성화
   - 스테이킹 가능 ✅
```

### 2.5 가스 비용 분석

**한 번의 롤오버 비용:**
```
_rolloverSeason() 작업:
- _finalizeSeasonAggregation(): ~30,000 가스
- 애드온 알림 (선택): ~50,000 가스
- 새 시즌 생성: ~40,000 가스
- 합계: ~120,000 가스/시즌
```

**다중 롤오버 비용:**
```
5개 시즌 건너뛰기:
- 5 × 120,000 = 600,000 가스

10개 시즌:
- 10 × 120,000 = 1,200,000 가스

50개 시즌 (최대):
- 50 × 120,000 = 6,000,000 가스
```

**최대 제한 이유:**
- 블록 가스 한도: 보통 30,000,000 가스
- 50개 시즌: ~6,000,000 가스 (여유 있음)
- 실제로는 1-2개만 롤오버됨
- 50개는 비정상 상황 (1.5년 이상 미사용)

### 2.6 복잡도 비교

| 방법 | 시간 복잡도 | 가스 비용 | 동작 |
|-----|-----------|----------|------|
| 단순 (1개씩) | O(1) | ~120,000 | ❌ 잘못된 동작 |
| While 루프 | O(N) | N × 120,000 | ✅ 정상 동작 |
| 수학적 계산 | O(1) | 낮음 | ⚠️ 복잡함, 이벤트 누락 |

**While 루프 선택 이유:**
1. **정확성**: 모든 시즌을 올바르게 생성
2. **이벤트**: 각 시즌별 이벤트 발생
3. **애드온**: 시즌 종료 알림 정상 동작
4. **단순성**: 이해하기 쉬운 로직
5. **실용성**: 대부분의 경우 1-2개만 처리

### 2.7 수학적 계산 방식과의 비교

**수학적 방식 (고려했지만 채택 안 함):**
```solidity
// 현재 블록이 속한 시즌 계산
uint elapsedBlocks = block.number - firstSeasonStart;
uint targetSeason = (elapsedBlocks / seasonBlocks) + 1;

// 중간 시즌 모두 생성
for (uint i = currentSeason + 1; i <= targetSeason; i++) {
    // 간소화된 시즌 생성
    seasons[i] = Season({
        seasonNumber: i,
        startBlock: ...,
        endBlock: ...,
        isFinalized: i < targetSeason,
        totalPoints: 0,
        seasonTotalStaked: 0,
        lastAggregatedBlock: ...,
        aggregatedPoints: 0
    });
}
```

**문제점:**
1. `nextSeasonStartBlock` 처리 복잡
2. 이벤트 발생 누락 (로그 추적 어려움)
3. 애드온 `onSeasonEnd` 호출 누락
4. 풀 종료 블록 처리 복잡
5. 코드 복잡도 증가

**결론:** While 루프가 더 명확하고 안전함

### 2.8 엣지 케이스 처리

**케이스 1: 시즌 갭이 설정된 경우**
```solidity
// nextSeasonStartBlock이 설정됨
if (nextSeasonStartBlock > 0 && block.number < nextSeasonStartBlock) {
    break;  // 다음 시즌 시작 대기
}
```

**케이스 2: 풀 종료 블록 도달**
```solidity
if (poolEndBlock > 0 && block.number >= poolEndBlock) {
    return;  // 더 이상 시즌 생성 안 함
}
```

**케이스 3: 50개 초과**
```solidity
require(rolloversPerformed < maxRollovers, "Too many seasons");
// 관리자가 manualRolloverSeasons() 호출하여 처리
```

### 2.9 수동 롤오버 함수

**50개 초과 시 해결 방법:**

```solidity
// 1. 대기 중인 시즌 개수 확인
uint pending = protocol.getPendingSeasonRollovers(projectID);
console.log("Pending seasons:", pending);  // 예: 150개

// 2. 수동으로 나눠서 롤오버
protocol.manualRolloverSeasons(projectID, 50);  // 1차: 50개
protocol.manualRolloverSeasons(projectID, 50);  // 2차: 50개  
protocol.manualRolloverSeasons(projectID, 50);  // 3차: 50개 (완료)

// 3. 정상 스테이킹 가능
router.stake{value: 100 ether}(projectID);
```

**함수 설명:**

```solidity
/**
 * @notice 수동으로 여러 시즌 롤오버 (관리자 전용)
 * @param maxRollovers 최대 롤오버 횟수
 * @return rolloversPerformed 실제로 수행된 롤오버 횟수
 */
function manualRolloverSeasons(uint maxRollovers) 
    external 
    onlyRole(MANAGER_ROLE) 
    returns (uint rolloversPerformed);

/**
 * @notice 롤오버가 필요한 시즌 개수 조회
 * @return pendingSeasons 롤오버 대기 중인 시즌 수
 */
function getPendingSeasonRollovers() 
    external 
    view 
    returns (uint pendingSeasons);
```

**사용 시나리오:**

```
상황: 프로젝트가 2년간 방치됨
- 시즌 길이: 30일 (2,592,000 블록)
- 경과 시간: 2년 = 24개월 = 약 730일
- 필요한 시즌: 730 / 30 = 24개 시즌

해결:
1. getPendingSeasonRollovers() 호출 → 24 반환
2. manualRolloverSeasons(50) 호출 → 24개 모두 롤오버
3. 정상 스테이킹 가능
```

**가스 비용:**
- 시즌당: ~120,000 가스
- 50개 시즌: ~6,000,000 가스
- 100개 시즌: ~12,000,000 가스 (2회 호출)

---

## 3. O(1) 시즌 집계 시스템

### 3.1 문제 분석

**단순 구현 (O(N)):**
```solidity
function getTotalPoints(uint season) public view returns (uint) {
    uint total = 0;
    for (uint i = 0; i < stakers.length; i++) {
        total += userSeasonData[season][stakers[i]].points;
    }
    return total;
}

// 문제:
// - 스테이커 1000명 → 1000번 루프
// - 가스 비용 매우 높음
// - 블록 가스 한도 초과 가능
```

### 3.2 수학적 최적화

**핵심 아이디어:**
```
모든 유저의 포인트 합:
Σ(유저별 잔액 × 시간) = (모든 유저 잔액의 합) × 시간
                        = 총 스테이킹 × 시간
```

**증명:**
```
3명의 유저:
- Alice: 50 CROSS, 100 블록 → 5,000 포인트
- Bob: 30 CROSS, 100 블록 → 3,000 포인트
- Charlie: 20 CROSS, 100 블록 → 2,000 포인트

개별 합계: 5,000 + 3,000 + 2,000 = 10,000

집계 계산: (50 + 30 + 20) × 100 = 100 × 100 = 10,000 ✓
```

### 3.3 구현

**Season 구조체:**
```solidity
struct Season {
    uint seasonNumber;
    uint startBlock;
    uint endBlock;
    bool isFinalized;
    uint totalPoints;           // 최종 총 포인트 (캐시)
    uint seasonTotalStaked;     // 집계용 총 스테이킹
    uint lastAggregatedBlock;   // 마지막 집계 블록
    uint aggregatedPoints;      // 누적 집계 포인트
}
```

**증분 업데이트:**
```solidity
function _updateSeasonAggregation(uint seasonNum) internal {
    Season storage season = seasons[seasonNum];
    
    // 이미 최신이면 스킵
    if (season.lastAggregatedBlock >= block.number) return;
    
    // 총 스테이킹이 0이면 시간만 업데이트
    if (season.seasonTotalStaked == 0) {
        season.lastAggregatedBlock = block.number;
        return;
    }
    
    // 증분 포인트 계산
    uint additionalPoints = PointsLib.calculatePoints(
        season.seasonTotalStaked,
        season.lastAggregatedBlock,
        block.number,
        blockTime,
        pointsTimeUnit
    );
    
    // 누적
    season.aggregatedPoints += additionalPoints;
    season.lastAggregatedBlock = block.number;
}
```

**스테이킹 시 업데이트:**
```solidity
function _stakeFor(...) internal {
    // ... 기존 로직 ...
    
    // 집계 업데이트 (현재까지)
    _updateSeasonAggregation(currentSeason);
    
    // 총 스테이킹 증가
    currentSeasonData.seasonTotalStaked += amount;
    
    // 이후 블록부터 새로운 총량으로 계산됨
}
```

**시뮬레이션:**
```
블록 1000: 시즌 시작
- seasonTotalStaked: 0
- aggregatedPoints: 0
- lastAggregatedBlock: 1000

블록 1100: Alice 100 스테이킹
- 집계 업데이트: 0 × (1100-1000) = 0
- seasonTotalStaked: 0 → 100
- aggregatedPoints: 0
- lastAggregatedBlock: 1100

블록 1200: Bob 50 스테이킹
- 집계 업데이트: 100 × (1200-1100) = 10,000
- seasonTotalStaked: 100 → 150
- aggregatedPoints: 0 + 10,000 = 10,000
- lastAggregatedBlock: 1200

블록 1300: 조회
- 증분: 150 × (1300-1200) = 15,000
- 총 포인트: 10,000 + 15,000 = 25,000

검증:
- Alice: 100 × (1300-1100) = 20,000
- Bob: 50 × (1300-1200) = 5,000
- 합계: 25,000 ✓
```

### 3.4 복잡도 비교

| 방법 | 시간 복잡도 | 가스 비용 | 확장성 |
|-----|-----------|----------|--------|
| 단순 합계 | O(N) | N × 5,000 | 스테이커 수 제한 |
| 집계 방식 | O(1) | ~20,000 | 무제한 |

---

## 4. 지연 스냅샷 시스템

### 4.1 문제: 시즌 전환 시 가스 폭탄

**즉시 스냅샷 방식:**
```solidity
function _rolloverSeason() internal {
    // 모든 스테이커의 시즌 데이터 생성
    for (uint i = 0; i < stakers.length; i++) {
        _snapshotUser(stakers[i], oldSeason);
    }
    
    // 문제:
    // - 스테이커 1000명 → 1000번 스냅샷
    // - 가스 비용 수백만
    // - 블록 가스 한도 초과
    // - 시즌 전환 불가능
}
```

### 4.2 해결: Lazy Evaluation

**핵심 아이디어:**
```
시즌 종료 시:
- 아무것도 하지 않음 (O(1))

보상 청구/스테이킹 시:
- 해당 유저의 이전 시즌만 계산
- 필요한 시즌만 처리
- 개별 유저가 가스 부담
```

### 4.3 구현

**UserSeasonData:**
```solidity
struct UserSeasonData {
    uint points;        // 시즌 포인트
    uint balance;       // 시즌 잔액
    uint joinBlock;     // 참여 블록
    bool claimed;       // 청구 여부
    bool finalized;     // 스냅샷 완료 여부
}
```

**지연 스냅샷:**
```solidity
function _ensureUserAllPreviousSeasons(address user) internal {
    if (currentSeason == 0) return;
    
    // 마지막으로 처리한 시즌
    uint startSeason = lastFinalizedSeason[user];
    if (startSeason == 0) startSeason = 1;
    
    if (startSeason >= currentSeason) return;
    
    // 이전 시즌들을 순회하며 스냅샷
    for (uint i = startSeason; i < currentSeason;) {
        _ensureUserSeasonSnapshot(user, i);
        unchecked { ++i; }
    }
    
    lastFinalizedSeason[user] = currentSeason - 1;
}
```

**개별 스냅샷:**
```solidity
function _ensureUserSeasonSnapshot(address user, uint seasonNum) internal {
    Season storage season = seasons[seasonNum];
    
    // 시즌이 종료되지 않았으면 스킵
    if (!season.isFinalized) return;
    
    UserSeasonData storage userData = userSeasonData[seasonNum][user];
    
    // 이미 처리됐으면 스킵
    if (userData.finalized) return;
    
    StakePosition storage position = userStakes[user];
    
    // 스테이킹이 없었으면 스킵
    if (position.balance == 0 && userData.balance == 0) {
        userData.finalized = true;
        return;
    }
    
    // 포인트 계산 (소급)
    uint balanceToUse = userData.balance > 0 
        ? userData.balance 
        : position.balance;
    
    uint joinBlockToUse = userData.joinBlock > 0 
        ? userData.joinBlock 
        : position.lastUpdateBlock;
    
    // 시즌 범위 내에서 포인트 계산
    if (joinBlockToUse < season.startBlock) {
        userData.balance = balanceToUse;
        userData.joinBlock = season.startBlock;
        userData.points = PointsLib.calculatePoints(
            balanceToUse,
            season.startBlock,
            season.endBlock,
            blockTime,
            pointsTimeUnit
        );
    } else if (joinBlockToUse <= season.endBlock) {
        userData.balance = balanceToUse;
        userData.joinBlock = joinBlockToUse;
        userData.points = PointsLib.calculatePoints(
            balanceToUse,
            joinBlockToUse,
            season.endBlock,
            blockTime,
            pointsTimeUnit
        );
    }
    
    userData.finalized = true;
}
```

### 4.4 예시 실행 흐름

**시나리오:**
```
블록 1000: 시즌 1 시작
블록 1500: Alice 100 스테이킹
블록 3000: 시즌 1 종료, 시즌 2 시작
블록 3500: Alice 추가 스테이킹 50
블록 5000: 시즌 2 종료, 시즌 3 시작
블록 5500: Alice 보상 청구 (시즌 1)
```

**트랜잭션별 처리:**

**블록 1500 (첫 스테이킹):**
```solidity
stake(100)
  ↓
_ensureUserAllPreviousSeasons(Alice)
  ↓ currentSeason = 1
  ↓ lastFinalizedSeason[Alice] = 0
  ↓ 처리할 시즌 없음
  ↓
userStakes[Alice].balance = 100
userSeasonData[1][Alice].balance = 100
userSeasonData[1][Alice].joinBlock = 1500
```

**블록 3000 (시즌 전환):**
```solidity
// 자동 롤오버
_rolloverSeason()
  ↓
seasons[1].isFinalized = true
seasons[2] 생성
  ↓
// Alice의 시즌 1 데이터는 아직 finalized = false
```

**블록 3500 (추가 스테이킹):**
```solidity
stake(50)
  ↓
_ensureUserAllPreviousSeasons(Alice)
  ↓ currentSeason = 2
  ↓ lastFinalizedSeason[Alice] = 0
  ↓ 처리 범위: 시즌 1
  ↓
_ensureUserSeasonSnapshot(Alice, 1)
  ↓ season 1 finalized = true ✓
  ↓ userData finalized = false ✓
  ↓ 포인트 계산: 100 × (3000-1500) = 150,000
  ↓ userData[1].points = 150,000
  ↓ userData[1].finalized = true
  ↓
lastFinalizedSeason[Alice] = 1
  ↓
// 이제 시즌 2에서 스테이킹 계속
userStakes[Alice].balance = 150
userSeasonData[2][Alice].balance = 150
userSeasonData[2][Alice].joinBlock = 3500
```

**블록 5500 (보상 청구):**
```solidity
claimSeason(1, token)
  ↓
_ensureUserSeasonSnapshot(Alice, 1)
  ↓ 이미 finalized = true
  ↓ 스킵
  ↓
uint userPoints = userSeasonData[1][Alice].points  // 150,000
uint totalPoints = seasons[1].totalPoints
  ↓
rewardPool.payUser(Alice, 1, token, userPoints, totalPoints)
```

### 4.5 가스 비용 분석

| 시나리오 | 즉시 스냅샷 | 지연 스냅샷 |
|---------|-----------|-----------|
| 시즌 전환 | O(N) × 100,000 | O(1) × 20,000 |
| 첫 청구 (10 시즌) | 0 | 10 × 20,000 = 200,000 |
| 이후 청구 | 80,000 | 80,000 |
| 총합 (미청구 포함) | O(N) × 100,000 | 개별 부담 |

**장점:**
- 시즌 전환 항상 성공
- 미청구 유저는 가스 부담 없음
- 확장성 무한대

---

## 5. Transient 재진입 방지 (EIP-1153)

### 5.1 기존 방식의 문제

**영구 스토리지 방식:**
```solidity
contract ReentrancyGuard {
    uint256 private _status;
    
    modifier nonReentrant() {
        require(_status != 2, "ReentrancyGuard: reentrant call");
        _status = 2;  // SSTORE (20,000 가스)
        _;
        _status = 1;  // SSTORE (20,000 가스)
    }
}

// 비용: 40,000 가스
```

### 5.2 Transient Storage (EIP-1153)

**개념:**
- 트랜잭션 수명 동안만 유효
- 트랜잭션 종료 시 자동 삭제
- 저장 비용 낮음

**OpenZeppelin 구현:**
```solidity
abstract contract ReentrancyGuardTransient {
    // 트랜잭션 동안만 유효한 스토리지
    uint256 private constant NOT_ENTERED = 0;
    uint256 private constant ENTERED = 1;
    
    modifier nonReentrant() {
        require(_reentrancyGuardEntered() == NOT_ENTERED);
        _setReentrancyGuardEntered(ENTERED);
        _;
        _setReentrancyGuardEntered(NOT_ENTERED);
    }
    
    function _reentrancyGuardEntered() private view returns (uint256) {
        uint256 value;
        assembly {
            value := tload(0)  // TLOAD (100 가스)
        }
        return value;
    }
    
    function _setReentrancyGuardEntered(uint256 value) private {
        assembly {
            tstore(0, value)  // TSTORE (100 가스)
        }
    }
}

// 비용: 200 가스 (200배 절약!)
```

### 5.3 적용

```solidity
abstract contract CrossStakingBase is 
    AccessControlDefaultAdminRules,
    ReentrancyGuardTransient  // ← Transient 사용
{
    // ...
}

// 모든 하위 컨트랙트에 적용
contract StakingPool is StakingPoolBase {
    function stake(uint amount) external nonReentrant { ... }
    function withdrawAll() external nonReentrant { ... }
    function claimSeason(...) external nonReentrant { ... }
}
```

---

## 6. SafeERC20과 토큰 안전성

### 6.1 문제: 비표준 ERC20 토큰

**일반적인 문제들:**
```solidity
// 1. 반환값 없음 (USDT)
function transfer(address to, uint amount);  // bool 반환 안 함

// 2. revert 대신 false 반환
function transfer(address to, uint amount) returns (bool) {
    if (balanceOf[msg.sender] < amount) return false;  // revert 안 함
}

// 3. 수수료 토큰
function transfer(address to, uint amount) returns (bool) {
    uint fee = amount / 100;
    balanceOf[to] += amount - fee;  // 받는 금액이 다름
}
```

### 6.2 SafeERC20 사용

```solidity
import "@openzeppelin/contracts/token/ERC20/utils/SafeERC20.sol";

contract StakingPool {
    using SafeERC20 for IERC20;
    
    function stake(uint amount) external {
        // 안전한 전송
        stakingToken.safeTransferFrom(msg.sender, address(this), amount);
        
        // SafeERC20가 처리:
        // 1. 반환값 확인
        // 2. revert 보장
        // 3. approve 안전성
    }
}
```

**SafeTransferFrom 내부:**
```solidity
function safeTransferFrom(IERC20 token, address from, address to, uint value) {
    _callOptionalReturn(
        token,
        abi.encodeWithSelector(token.transferFrom.selector, from, to, value)
    );
}

function _callOptionalReturn(IERC20 token, bytes memory data) private {
    bytes memory returndata = address(token).functionCall(data);
    
    if (returndata.length > 0) {
        // 반환값이 있으면 true인지 확인
        require(abi.decode(returndata, (bool)), "SafeERC20: operation failed");
    }
    // 반환값이 없어도 revert 안 했으면 성공으로 간주
}
```

### 6.3 전체 적용

```solidity
abstract contract CrossStakingBase {
    using SafeERC20 for IERC20;
}

// 모든 토큰 전송에 적용
stakingToken.safeTransferFrom(from, address(this), amount);
stakingToken.safeTransfer(recipient, amount);
IERC20(token).safeTransferFrom(msg.sender, address(this), amount);
IERC20(token).safeTransfer(user, amount);
IERC20(address(wcross)).safeIncreaseAllowance(stakingPool, amount);
```

---

## 7. Immutable 변수 최적화

### 7.1 Immutable vs Storage

**비교:**

| 타입 | 저장 위치 | 읽기 비용 | 쓰기 비용 |
|-----|---------|----------|----------|
| Storage | 스토리지 슬롯 | 2,100 가스 | 20,000 가스 |
| Immutable | 코드 내 | 3 가스 | 배포 시 한 번 |

**예시:**
```solidity
contract Example {
    // Storage 변수
    IERC20 public stakingToken;  // 2,100 가스/읽기
    
    // Immutable 변수
    IERC20 public immutable stakingToken;  // 3 가스/읽기
}
```

### 7.2 프로젝트 적용

```solidity
contract StakingPool {
    // 불변 값들은 immutable로
    IERC20 public immutable stakingToken;
    uint public immutable projectID;
    IStakingProtocol public immutable protocol;
    
    // 변경 가능한 값들은 storage
    IRewardPool public rewardPool;
    uint public currentSeason;
    uint public totalStaked;
}

contract RewardPool {
    IStakingPool public immutable stakingPool;
    IStakingProtocol public immutable protocol;
}

contract StakingRouter {
    WCROSS public immutable wcross;
    IStakingProtocol public immutable protocol;
}
```

**효과:**
```
100번의 stakingToken 읽기:
- Storage: 100 × 2,100 = 210,000 가스
- Immutable: 100 × 3 = 300 가스
- 절약: 209,700 가스 (700배!)
```

---

## 8. Unchecked 산술 최적화

### 8.1 Solidity 0.8+ 오버플로우 체크

**기본 동작:**
```solidity
function increment(uint i) public pure returns (uint) {
    return i + 1;  // 오버플로우 체크 추가 (~20 가스)
}

// 컴파일러가 자동 삽입:
function increment(uint i) public pure returns (uint) {
    unchecked {
        uint result = i + 1;
        require(result > i, "Overflow");  // 추가 코드
        return result;
    }
}
```

### 8.2 안전한 경우 Unchecked 사용

**루프 카운터:**
```solidity
// 안전하지 않음 (불필요한 체크)
for (uint i = 0; i < 100; i++) {
    // ...
}

// 최적화
for (uint i = 0; i < 100;) {
    // ...
    unchecked { ++i; }  // 오버플로우 불가능
}
```

### 8.3 프로젝트 적용

```solidity
function _ensureUserAllPreviousSeasons(address user) internal {
    uint startSeason = lastFinalizedSeason[user];
    if (startSeason == 0) startSeason = 1;
    
    for (uint i = startSeason; i < currentSeason;) {
        _ensureUserSeasonSnapshot(user, i);
        unchecked {
            ++i;  // i는 항상 currentSeason 미만
        }
    }
}

function claimMultipleRewards(...) external {
    for (uint i = 0; i < seasons.length;) {
        IStakingPool(stakingPool).claimSeason(seasons[i], rewardTokens[i]);
        unchecked {
            ++i;  // 배열 길이 내에서만 증가
        }
    }
}
```

**효과:**
- 루프당 ~20 가스 절약
- 긴 루프에서 큰 효과

---

## 9. Hook 패턴과 확장성

### 9.1 Template Method 패턴

**개념:**
```
Base 컨트랙트에서 알고리즘 골격 정의
구체적인 단계는 하위 클래스에서 구현
```

**구현:**

```solidity
abstract contract StakingPoolBase {
    function _stakeFor(address user, uint amount, address from) internal {
        // 고정된 알고리즘
        _ensureSeason();
        
        // Hook 1: 스테이킹 전 처리
        _beforeStake(user, amount, oldBalance);
        
        // 핵심 로직
        _performStake(user, amount, from);
        
        // Hook 2: 스테이킹 후 처리
        _afterStake(user, amount, newBalance);
    }
    
    // Hook 함수들 (오버라이드 가능)
    function _beforeStake(address user, uint amount, uint oldBalance) 
        internal virtual {}
    
    function _afterStake(address user, uint amount, uint newBalance) 
        internal virtual {}
}

contract StakingPool is StakingPoolBase {
    // Hook 오버라이드
    function _afterStake(address user, uint amount, uint newBalance) 
        internal virtual override 
    {
        // Addon 알림
        if (address(stakingAddon) != address(0)) {
            _callAddonSafe(
                stakingAddon,
                abi.encodeWithSignature(
                    "onStake(address,uint256,uint256,uint256,uint256)",
                    user, amount, oldBalance, newBalance, currentSeason
                )
            );
        }
    }
}
```

### 9.2 확장 예시: 최소 보유 기간

```solidity
contract TimeLockStakingPool is StakingPool {
    mapping(address => uint) public stakingStartTime;
    uint public constant MIN_STAKING_PERIOD = 7 days;
    
    function _afterStake(address user, uint amount, uint newBalance) 
        internal override 
    {
        // 기존 Hook 실행
        super._afterStake(user, amount, newBalance);
        
        // 추가 로직
        if (stakingStartTime[user] == 0) {
            stakingStartTime[user] = block.timestamp;
        }
    }
    
    function _beforeWithdraw(address user, uint amount) 
        internal override 
    {
        // 보유 기간 확인
        require(
            block.timestamp >= stakingStartTime[user] + MIN_STAKING_PERIOD,
            "Minimum staking period not met"
        );
        
        super._beforeWithdraw(user, amount);
    }
}
```

### 9.3 확장 예시: 보너스 보상

```solidity
contract BonusRewardPool is RewardPool {
    // 장기 보유 보너스: 10%
    uint public constant LONG_TERM_BONUS = 10;  // 10%
    uint public constant LONG_TERM_THRESHOLD = 30 days;
    
    function _calculateBonusReward(
        address user,
        uint season,
        address token,
        uint userPoints,
        uint totalPoints,
        uint baseReward
    ) internal override returns (uint bonusAmount) {
        // 장기 보유 여부 확인
        (,, uint joinBlock,,) = stakingPool.getUserSeasonData(season, user);
        Season memory seasonData = stakingPool.seasons(season);
        
        uint stakingDuration = (seasonData.endBlock - joinBlock) * blockTime;
        
        if (stakingDuration >= LONG_TERM_THRESHOLD) {
            // 10% 보너스
            bonusAmount = baseReward * LONG_TERM_BONUS / 100;
        }
        
        return bonusAmount;
    }
}
```

---

## 10. Addon 시스템 아키텍처

### 10.1 인터페이스 설계

```solidity
interface IStakingAddon {
    function onStake(
        address user,
        uint amount,
        uint oldBalance,
        uint newBalance,
        uint season
    ) external;
    
    function onWithdraw(address user, uint amount, uint season) external;
    function onSeasonEnd(uint season, uint totalStaked, uint totalPoints) external;
    function onClaim(address user, uint season, uint points, uint rewardAmount) external;
}
```

### 10.2 안전 호출 메커니즘

**문제:**
```solidity
// Addon 실패 시 전체 트랜잭션 실패
function _afterStake(...) internal {
    stakingAddon.onStake(...);  // 여기서 revert → 스테이킹 실패
}
```

**해결:**
```solidity
function _afterStake(...) internal {
    if (address(stakingAddon) != address(0)) {
        _callAddonSafe(stakingAddon, data);  // 실패해도 계속 진행
    }
}

function _callAddonSafe(IStakingAddon addon, bytes memory data) private {
    try this.callAddon(addon, data) {}
    catch Error(string memory reason) {
        emit AddonCallFailed(addon, bytes4(data), reason);
    }
    catch {
        emit AddonCallFailed(addon, bytes4(data), "Unknown error");
    }
}

// External 래퍼 (try/catch를 위해 필요)
function callAddon(IStakingAddon addon, bytes memory data) external {
    require(msg.sender == address(this), "Only self");
    (bool success, bytes memory returnData) = address(addon).call(data);
    if (!success) {
        // revert 전파
        if (returnData.length > 0) {
            assembly {
                revert(add(32, returnData), mload(returnData))
            }
        } else {
            revert("Addon call failed");
        }
    }
}
```

### 10.3 Allowlist 시스템

**보안 문제:**
```solidity
// 나쁜 예: 아무 Addon이나 설정 가능
function setStakingAddon(IStakingAddon addon) external onlyAdmin {
    stakingAddon = addon;  // 악의적인 컨트랙트 가능
}
```

**해결:**
```solidity
mapping(address => bool) public approvedAddons;

function setAddonApproved(IStakingAddon addon, bool approved) 
    external onlyRole(DEFAULT_ADMIN_ROLE) 
{
    approvedAddons[address(addon)] = approved;
}

function setStakingAddon(IStakingAddon newAddon) 
    external onlyRole(DEFAULT_ADMIN_ROLE) 
{
    require(
        address(newAddon) == address(0) || 
        approvedAddons[address(newAddon)],
        "Addon not approved"
    );
    stakingAddon = newAddon;
}
```

---

## 11. 가스 최적화 체크리스트

### 11.1 스토리지 최적화

- [x] Immutable 변수 사용 (stakingToken, projectID, protocol)
- [x] Packed storage (Season 구조체)
- [x] Transient storage (재진입 방지)
- [x] Storage 읽기 최소화 (로컬 변수 캐싱)

### 11.2 루프 최적화

- [x] Unchecked 증가 (루프 카운터)
- [x] 배열 길이 캐싱
- [x] Break 조건 최적화
- [x] 불필요한 루프 제거 (O(1) 집계)

### 11.3 함수 최적화

- [x] External vs Public (external 우선)
- [x] View/Pure 함수 (상태 변경 없음)
- [x] Short-circuit evaluation
- [x] 조기 반환 (early return)

### 11.4 데이터 구조 최적화

- [x] Mapping vs Array (조회 빈번)
- [x] 중첩 매핑 (다차원 데이터)
- [x] 비트 패킹 (bool 플래그들)
- [x] 삭제 후 환불 (SSTORE 0)

### 11.5 알고리즘 최적화

- [x] O(N) → O(1) (시즌 집계)
- [x] 지연 평가 (스냅샷)
- [x] 증분 업데이트 (집계)
- [x] 배치 처리 (다중 청구)

---

## 12. Safe Math 및 오버플로우 안전성

### 12.1 Solidity 0.8+ 내장 체크

**기본 보호:**
```solidity
pragma solidity ^0.8.13;

// 자동 오버플로우 체크
uint a = type(uint).max;
uint b = a + 1;  // ❌ Revert (Panic: 0x11)

// 자동 언더플로우 체크
uint c = 0;
uint d = c - 1;  // ❌ Revert (Panic: 0x11)
```

### 12.2 포인트 계산 안전성 분석

**PointsLib.calculatePoints():**
```solidity
function calculatePoints(...) internal pure returns (uint points) {
    uint blockElapsed = toBlock - fromBlock;
    uint timeElapsed = blockElapsed * blockTime;
    
    // 오버플로우 위험 분석:
    // balance * timeElapsed * POINTS_PRECISION
    return (balance * timeElapsed * POINTS_PRECISION) / timeUnit;
}
```

**안전성 증명:**
```
최대 연산:
- balance: ~10^27 (토큰 공급량, 예: 10^9 * 10^18)
- timeElapsed: ~10^9 (수백만 블록 * 블록 시간)
- POINTS_PRECISION: 10^6

곱셈 결과: 10^27 * 10^9 * 10^6 = 10^42
uint256 최대값: ~1.15 * 10^77

결론: 안전 여유도 10^35배 → 오버플로우 불가능 ✅
```

**주석 명시:**
```solidity
/**
 * @dev Overflow 안정성:
 *      - Solidity 0.8+ 내장 오버플로우 체크 사용
 *      - 최대 연산: balance(~10^27) * timeElapsed(~10^9) * PRECISION(10^6) = ~10^42
 *      - uint256 최대값: ~10^77 이므로 안전함
 *      - 연산 순서: 곱셈 먼저 수행하여 정밀도 유지, 마지막에 나눗셈
 */
```

### 12.3 보상 분배 안전성

**PointsLib.calculateProRata():**
```solidity
function calculateProRata(...) internal pure returns (uint userReward) {
    if (totalAmount == 0 || userAmount == 0) return 0;
    return (rewardAmount * userAmount) / totalAmount;
}
```

**안전성 증명:**
```
조건:
- userAmount ≤ totalAmount (보장됨)
- rewardAmount * userAmount ≤ rewardAmount * totalAmount

결론: 분자가 분모보다 작거나 같으므로 오버플로우 위험 없음 ✅
```

### 12.4 집계 시스템 안전성

**시즌 집계 업데이트:**
```solidity
function _updateSeasonAggregation(uint seasonNum) internal {
    uint additionalPoints = PointsLib.calculatePoints(...);
    
    // 증분 업데이트 (오버플로우 체크 자동)
    season.aggregatedPoints += additionalPoints;  // ✅ 0.8+ 자동 체크
    season.lastAggregatedBlock = block.number;
}
```

**보호 메커니즘:**
1. Solidity 0.8+ 자동 체크
2. 현실적 범위 내 연산
3. 증분 업데이트 (누적은 한정적)

### 12.5 언제 Unchecked를 사용하는가?

**안전한 Unchecked 사용:**
```solidity
// ✅ 루프 카운터 (범위 제한됨)
unchecked {
    ++i;
}

// ✅ 이미 검증된 값
if (a > b) {
    unchecked {
        uint diff = a - b;  // 안전함
    }
}
```

**절대 하지 말아야 할 것:**
```solidity
// ❌ 사용자 입력
unchecked {
    uint result = userInput1 * userInput2;  // 위험!
}

// ❌ 토큰 잔액
unchecked {
    balance += amount;  // 위험!
}
```

### 12.6 정밀도 vs 오버플로우 트레이드오프

**현재 선택:**
```solidity
POINTS_PRECISION = 1e6  // 소수점 6자리
```

**이유:**
- 충분한 정밀도 (0.000001 단위)
- 오버플로우 안전 여유도 충분
- 가스 효율적

**대안 검토:**
```
1e18 (높은 정밀도):
- 장점: 매우 정밀
- 단점: 오버플로우 위험 증가 (여유도 10^17배 감소)

1e3 (낮은 정밀도):
- 장점: 오버플로우 매우 안전
- 단점: 정밀도 부족 (0.001 단위)

결론: 1e6이 최적 ✅
```

### 12.7 요약

**안전성 계층:**
1. ✅ Solidity 0.8+ 자동 오버플로우 체크
2. ✅ 현실적 범위 내 연산 설계
3. ✅ 명시적 안전성 주석
4. ✅ 검증된 수학적 증명
5. ✅ 전체 테스트 커버리지

**결론:** Cross Staking은 오버플로우로부터 완전히 안전합니다. 🛡️

---

## 요약

Cross Staking 프로젝트는 다음 기술들을 활용하여 확장 가능하고 가스 효율적인 스테이킹 플랫폼을 구현했습니다:

1. **Code 패턴**: 컨트랙트 크기 제한 극복
2. **다중 시즌 롤오버**: 여러 시즌 자동 처리 (최대 50개)
3. **O(1) 집계**: 시즌 총 포인트 계산 최적화
4. **지연 스냅샷**: 시즌 전환 가스 비용 최소화
5. **Transient Storage**: 재진입 방지 가스 절약
6. **SafeERC20**: 토큰 안전성 보장
7. **Immutable**: 변수 읽기 비용 최소화
8. **Unchecked**: 안전한 산술 최적화
9. **Hook 패턴**: 확장 가능한 아키텍처
10. **Addon 시스템**: 독립적인 기능 추가
11. **종합 최적화**: 다층 가스 최적화
12. **Safe Math**: 오버플로우 완전 방지

이러한 기술들의 조합으로 수천 명의 스테이커를 지원하면서도 합리적인 가스 비용을 유지합니다.

